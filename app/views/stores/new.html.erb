<h1>Add Hockey Store</h1>

<%= form_for(@store) do |f| %>
	<p><%= f.label(:city) %><br />
	<%= f.text_field(:city) %></p>
	<p><%= f.label(:state) %><br />
	<%= f.text_field(:state) %></p>

	<%= submit_tag("Add Store") %>
<% end %>